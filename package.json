{
  "name": "coinleagues",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ DexKit/coinleagues",
    "create-local": "graph create --node http://localhost:8020/ DexKit/coinleagues",
    "remove-local": "graph remove --node http://localhost:8020/ DexKit/coinleagues",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 DexKit/coinleagues"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.2",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
